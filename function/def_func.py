def add_times(a, b, c):
    return (a + b) * c


def get_integer(prompt='Type an integer:'):
    """Ask the user  for a integer, and return it as  an int"""
    response = input(prompt)
    return int(response)
